<% if current_user.role == 'admin' || current_user.role == 'premium' %>
    <div class="form-group">
      <%= f.label :isprivate, class: 'checkbox' do %>
      <%= f.check_box :isprivate %> Private wiki
    <% end %>
  </div>
<% else %>
<% end %>