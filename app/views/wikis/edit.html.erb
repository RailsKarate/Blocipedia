<h1>Edit Wiki</h1>
 
 <div class="row">
   <div class="col-md-4">
     <h3>Guidelines for wikis</h3>
     <ul>
       <li>Search Blocipedia in case an article already exists on the subject</li>
       <li>Practice editing already existing articles</li>
       <li>Gather sources</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @wiki do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter wiki body" %>
       </div>
       <% if current_user.role == 'admin' || current_user.role == 'premium'%>
          <div class="form-group">
            <%= f.label :Private%>
            <%= f.check_box :isprivate %>
          </div>
        <% end %>

     <div class="form-group">
      <%= f.submit "Update Wiki", class: 'btn btn-success'%>
         <% if current_user.role == 'premium'%>
          <%= link_to "Add/Delete Collaborator", new_wiki_collaborator_path(@wiki), class: 'btn btn-primary' %>
          <% end %>
       </div>
        
      <% end %>
   </div>
 </div>
