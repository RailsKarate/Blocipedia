<h1>Create A New Wiki</h1>
 
 <div class="row">
   <div class="col-md-4">
     <h3>Guidelines for wikis</h3>
     <ul>
       <li>Search Blocipedia in case an article already exists on the subject</li>
       <li>Practice editing already existing articles</li>
       <li>Gather sources</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @wiki do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title%>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body %>
       </div>
       <div class="form-group">
         <%= f.submit "Save" %>
       </div>
      <% if current_user.role == 'admin' || current_user.role == 'premium'%>
          <div class="form-group">
            <%= f.check_box :private, {}, false, true %> Private wiki
          </div>
        <% end %> 
     <% end %>
   </div>
 </div>
